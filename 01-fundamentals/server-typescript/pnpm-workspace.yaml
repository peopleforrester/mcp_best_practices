# ABOUTME: pnpm settings for the single TS package; overrides pin patched transitive dependencies.
# ABOUTME: These clear osv-scanner advisories in deps pulled by the MCP SDK and vitest.
packages:
  - .
overrides:
  fast-uri: ">=4.1.3"
  "@hono/node-server": ">=2.0.5"
  postcss: ">=8.5.18"
  hono: ">=4.13.5"
  ip-address: ">=10.3.1"
  nanoid: ">=3.3.18"
  qs: ">=6.16.0"
